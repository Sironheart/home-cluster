---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home
spec:
  secretName: home-tls
  issuerRef:
    name: le-self-signed
    kind: ClusterIssuer
  commonName: home
  dnsNames: ["home", "*.home"]

---
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: "${APP}"
spec:
  policyRef:
    name: "${APP}"
  schedule:
    concurrencyPolicy: Forbid
    cron: "${KOPIUR_CRON:=0 */12 * * *}"
    runOnCreate: false

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seerr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: default
  values:
    controllers:
      seerr:
        annotations:
          reloader.stakater.com/auto: "true"
        # initContainers:
        #   configure-oidc:
        #     image:
        #       repository: &imageRepo ghcr.io/seerr-team/seerr
        #       tag: &imageTag v3.2.0@sha256:c4cbd5121236ac2f70a843a0b920b68a27976be57917555f1c45b08a1e6b2aad
        #     env:
        #       OIDC_CLIENT_ID:
        #         valueFrom:
        #           secretKeyRef:
        #             name: seer-app-kanidm-oauth2-credentials
        #             key: CLIENT_ID
        #       OIDC_CLIENT_SECRET:
        #         valueFrom:
        #           secretKeyRef:
        #             name: seer-app-kanidm-oauth2-credentials
        #             key: CLIENT_SECRET
        #     command:
        #       - /bin/sh
        #       - -ec
        #     args:
        #       - |
        #         node <<'EOF'
        #         const fs = require('fs');
        #
        #         const applicationUrl = 'https://request.siron.casa';
        #         const issuerUrl = 'https://sso.siron.casa/oauth2/openid/seer-app/';
        #         const mediaServerTypeNotConfigured = 4;
        #         const settingsPath = '/app/config/settings.json';
        #
        #         if (!process.env.OIDC_CLIENT_ID || !process.env.OIDC_CLIENT_SECRET) {
        #           throw new Error('Missing Kanidm OIDC client credentials');
        #         }
        #
        #         const settings = fs.existsSync(settingsPath)
        #           ? JSON.parse(fs.readFileSync(settingsPath, 'utf8'))
        #           : {};
        #
        #         settings.main = {
        #           ...(settings.main ?? {}),
        #           applicationUrl,
        #           // oidcLogin: true,
        #           mediaServerType:
        #             settings.main?.mediaServerType ?? mediaServerTypeNotConfigured,
        #         };
        #
        #         const existingProviders = Array.isArray(settings.oidc?.providers)
        #           ? settings.oidc.providers
        #           : [];
        #         const currentProvider =
        #           existingProviders.find((provider) => provider?.slug === 'kanidm') ?? {};
        #
        #         settings.oidc = {
        #           ...(settings.oidc ?? {}),
        #           providers: [
        #             ...existingProviders.filter((provider) => provider?.slug !== 'kanidm'),
        #             {
        #               ...currentProvider,
        #               slug: 'kanidm',
        #               name: 'Kanidm',
        #               issuerUrl,
        #               clientId: process.env.OIDC_CLIENT_ID,
        #               clientSecret: process.env.OIDC_CLIENT_SECRET,
        #               scopes: 'openid profile email groups',
        #               newUserLogin: true,
        #             },
        #           ],
        #         };
        #
        #         fs.writeFileSync(settingsPath, JSON.stringify(settings, null, ' '));
        #         EOF
        containers:
          seerr:
            image:
              repository: ghcr.io/seerr-team/seerr
              tag: v3.2.0@sha256:c4cbd5121236ac2f70a843a0b920b68a27976be57917555f1c45b08a1e6b2aad
            env:
              LOG_LEVEL: "debug"
              PORT: &port 80
            envFrom:
              - secretRef:
                  name: seerr-secret
              - secretRef:
                  name: seerr-db-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/v1/status
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 11m
                memory: 394Mi
              limits:
                memory: 394Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      seerr:
        controller: seerr
        ports:
          http:
            port: *port
    route:
      seerr:
        hostnames:
          - request.siron.casa
        parentRefs:
          - name: external
            namespace: network
        rules:
          - backendRefs:
              - identifier: seerr
                port: *port
    persistence:
      config:
        existingClaim: seerr
        globalMounts:
          - path: /app/config
      config-cache:
        existingClaim: seerr-cache
        globalMounts:
          - path: /app/config/cache
      tmpfs:
        type: emptyDir
        advancedMounts:
          seerr:
            seerr:
              - path: /app/config/logs
                subPath: logs
              - path: /tmp
                subPath: tmp
